<% @users.each do |user| %>
  <p>
    <!-- TODO link to gravatar -->
    <!-- TODO instead of user.fname, link to @full_name method in helper later -->
    <%= link_to(user.fname, user_path(user.id))%> 
  </p>
<% end %>